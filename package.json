{
  "name": "jira-analytics-suite",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r run build",
    "start": "concurrently \"pnpm --filter api start\" \"pnpm --filter web preview\"",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
